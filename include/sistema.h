#ifndef SISTEMA_H
#define SISTEMA_H

/* Camada de interacao com o usuario.
   O menu interativo sera implementado na Semana 3 do plano; por enquanto
   este modulo concentra apenas a formatacao de saida compartilhada. */

/* Imprime o cabecalho do sistema no terminal. */
void exibir_cabecalho(void);

#endif /* SISTEMA_H */
